import * as React from "react";

interface Props {
  citizenId: string;
}

const RegisteredVehicles: React.FC<Props> = ({ citizenId }) => {
  return <div></div>;
};

export default RegisteredVehicles;
