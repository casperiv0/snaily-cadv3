
export const RanksArr = ["owner", "admin", "moderator", "user"];
export const enum Ranks {
  owner = "owner",
  admin = "admin",
  moderator = "moderator",
  user = "user",
}

export const enum Whitelist {
  accepted = "accepted",
  pending = "pending",
  declined = "declined",
}
